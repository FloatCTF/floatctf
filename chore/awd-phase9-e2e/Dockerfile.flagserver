# Phase 9 REAL HOST E2E — FlagServer image (bookworm glibc base; alpine cannot run glibc binaries)
FROM python:3.12-slim-bookworm
COPY bin/awd_flagserver /usr/local/bin/awd_flagserver
ENV LISTEN_ADDR=0.0.0.0:8080
EXPOSE 8080
ENTRYPOINT ["/usr/local/bin/awd_flagserver"]