export type AwdpInstances = {
  instance_id: string;
  event_id: string;
  owner_user_id?: string;
  owner_team_id?: string;
  created_at: string;
  run_id: string;
  gamebox_id: string;
  reset_count: number;
};
